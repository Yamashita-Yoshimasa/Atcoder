const row = 0;
const column = 0;
let TDarray = new Array(row);
for (let i = 0; i < row + 1; i++) {
  TDarray[i] = new Array(column);
  for (let column = 0; column <= column; column++) {
    TDarray[i][column] = 0;
  }
}
